class UserModel {
  String phone, firstName, lastName, address, gender, smsAlert, photo;
  int age;

  UserModel({
    this.firstName,
    this.lastName,
    this.address,
    this.gender,
    this.age,
    this.smsAlert,
    this.phone,
    this.photo,
  });
}
