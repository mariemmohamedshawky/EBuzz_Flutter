class UserModel {
  String phone, firstName, lastName, address, gender, smsAlert, photo;
  int age, id;
  double latitude, longitude;

  UserModel({
    this.id,
    this.firstName,
    this.lastName,
    this.address,
    this.gender,
    this.age,
    this.smsAlert,
    this.phone,
    this.photo,
    this.latitude,
    this.longitude,
  });
}
