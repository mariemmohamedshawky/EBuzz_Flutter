class PostModel {
  int id, cityId;
  String userName, userPhoto, description, postPhoto, city, date, status;

  PostModel({
    this.id,
    this.userName,
    this.userPhoto,
    this.description,
    this.postPhoto,
    this.city,
    this.cityId,
    this.date,
    this.status,
  });
}
